spring:
  application:
    name: msvc-eureka
  config:
    import: optional:configserver:http://54.89.174.3:8888   # Le decimos que este es la ruta en donde se encuentra el servidor de configuraci�n
