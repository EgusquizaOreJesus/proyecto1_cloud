spring:
  application:
    name: msvc-eureka
  config:
    import: optional:configserver:http://3.87.248.168:8888   # Le decimos que este es la ruta en donde se encuentra el servidor de configuraci�n
