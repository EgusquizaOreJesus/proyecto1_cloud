server:
  port: 8761

spring:
  application:
    name: msvc-eureka
eureka:
  instance:
    hostname: 54.242.109.22   # Nombre del host donde se encuentra el servidor de Eureka
  client:
    registerWithEureka: false  # No se registra a si mismo
    fetchRegistry: false        # No se registra a si mismo
    serviceUrl:
      defaultZone: http://54.242.109.22:8761/eureka # URL del servidor de Eureka
