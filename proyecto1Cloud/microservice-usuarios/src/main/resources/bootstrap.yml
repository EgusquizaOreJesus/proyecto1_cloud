my_ip:
  value: 54.87.3.184
my_ip_config:
  value: 54.236.5.210

spring:
  application:
    name: msvc-usuarios
  cloud:
    config:
      uri: http://${my_ip_config.value}:8081
      profile: dev

microservicio:
  base-url: http://${my_ip.value}:9092

frontend:
  base-url: http://localhost:7777
