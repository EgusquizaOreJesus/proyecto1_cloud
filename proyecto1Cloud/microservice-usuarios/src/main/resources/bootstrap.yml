my_ip:
  value: 34.224.1.226

my_ip_config:
  value: 100.28.129.190

spring:
  application:
    name: msvc-usuarios
  cloud:
    config:
      uri: http://${my_ip_config.value}:8081
      profile: dev

microservicio:
  base-url: http://${my_ip.value}:9092

frontend:
  base-url: http://localhost:7777
